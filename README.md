# BOM Financial Services — Next.js static export (Cloudflare Pages)

## Ontwikkelen
```bash
npm install
npm run dev
```

## Bouwen (statisch)
```bash
npm run build
npm run export
# Output in ./out
```

## Cloudflare Pages instellingen
- Framework preset: **None**
- Build command: `npm install && npm run build && npm run export`
- Build output directory: `out`
